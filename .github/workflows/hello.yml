name: Hello workflow
on:
   workflow_dispatch: 
jobs:
   greet:
      runs-on: ubuntu-latest
      steps:
        - name: say hello
          run: echo "hello suhanee"

        - name: say hello to user
          run: echo "hello $USER"
