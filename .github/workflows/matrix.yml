name: multiple version check
on:
  workflow_dispatch:

jobs:
   validate:
      runs-on: ubuntu-latest
      strategy:
        fail-fast: false
        matrix:
          python-version: ["3.9", "3.10","3.11","3.12","3.13"]
      steps:
         - name: checkout code
           run:  echo "hello suhanee"
         - name: echo "check the python version ${{ matrix.python-version }}"
           run:  echo "pthon-version ${{ matrix.python-version }}"
         - name: code deploy
           run:  echo "code deployed successfully"
